<% Object.keys(path).forEach((verb) => {
    const operation = path[verb];
    // Patch
    operation.title = operation.title || operation.summary;
%>
<%- include(operation_template, { url, verb, operation }) %>
<% }) %>
